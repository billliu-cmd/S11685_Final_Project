# Core scientific stack
numpy>=1.24,<3
pandas>=2.0,<3
scipy>=1.10,<2

# Data and training utilities
yfinance>=0.2.40,<0.3
tqdm>=4.66,<5
matplotlib>=3.7,<4

# Notebook support
ipykernel>=6.29,<7
ipywidgets>=8.1,<9
nbformat>=5.10,<6
nbconvert>=7.16,<8

# Note:
# Torch is intentionally not pinned here because the correct wheel depends on
# the runtime (local CPU, local CUDA, or Colab GPU). The notebooks assume
# PyTorch is already available in the environment.
